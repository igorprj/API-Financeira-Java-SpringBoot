spring:
  application:
    name: Financeira
  datasource:
    url: jdbc:postgresql://localhost:5432/financeira
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres

    jpa:
      database-platform: org.hibernate.dialect.PostgreSQLDialect
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true

jwt:
  expiration: 3600000
  secret: MinhaChaveJWTSuperSecretaParaAPIBancaria2026ComMaisDe32Caracteres
